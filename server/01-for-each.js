const letters = ['a', 'b', 'c'];

for (let index = 0; index < letters.length; index++) {
    const element = letters[index];
    console.log(element);
}
console.log('===================')
//Reducing with arrays methods.
letters.forEach(el => console.log(el));

