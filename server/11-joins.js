const elements = ['fire', 'Air', 'Water', 'Earth'];

const rta = elements.join('--');
console.log('rta', rta);

//Example 2.
const title = 'Curso de manipulación de arrays';
const splited = title.split(' ');
const urlFinal = splited.join('-').toLowerCase();
console.log(urlFinal);